# Space, Self. Frontend

Next.js frontend for the digital workbook prototype.

## Run

```bash
npm install
npm run dev
```

By default the frontend expects the API at `http://localhost:8000`.
You can override it with:

```bash
NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev
```
